[package]
name = "os"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4.22"
sbi-rt = { version = "0.0.3", features = ["legacy"] }

[[bin]]
name = "os"
path = "src/main.rs"
test = false
doctest = false
bench = false
